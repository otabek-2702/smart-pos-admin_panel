# Production build — bake the local backend URL into the dist bundle.
# Every request from the built panel will go straight to 127.0.0.1:8000
# (desktop / single-box deploy where the Django backend runs alongside
# the static panel).
VITE_API_HOST=http://127.0.0.1:8000
