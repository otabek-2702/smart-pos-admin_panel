/* Single source of truth lives in ../utils/useWidth.ts.
 * Re-exported here so existing relative imports in this folder keep working
 * without forcing every chart component to bump its path. */
export { useWidth } from '../utils/useWidth'
